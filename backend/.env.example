# Copy this file to backend/.env (or create .env.backend at the project root)
# and fill in the Oracle credentials plus the SQL queries you want to expose.

ORACLE_USER=VIASOFT
ORACLE_PASSWORD=your_password_here
# Optional: override default connection string or driver path
# ORACLE_DSN=jdbc:oracle:thin:@(description=...)
# ORACLE_JAR_PATH=C:/AppCoopagricola/ojdbc8.jar
# ORACLE_JAR_URL=https://repo1.maven.org/maven2/com/oracle/database/jdbc/ojdbc8/19.3.0.0/ojdbc8-19.3.0.0.jar

# Provide the SQL that should return the fields consumed by the frontend.
# The backend automatically wraps each statement with
#   SELECT * FROM (<your statement>) WHERE ROWNUM <= ?
# so you do not need to add your own LIMIT clause.
# Example queries below are placeholders; adjust them to match your schema.
# AGROPLAN_QUERY_CULTIVARES=SELECT campo1 AS cultivar, campo2 AS area, ... FROM sua_tabela
# AGROPLAN_QUERY_ADUBACOES=SELECT ...
# AGROPLAN_QUERY_DEFENSIVOS=SELECT ...

# Optional: restrict CORS
# AGROPLAN_API_ALLOWED_ORIGINS=http://localhost:8081
